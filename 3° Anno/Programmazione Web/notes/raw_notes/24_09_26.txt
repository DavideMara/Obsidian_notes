# HTML
Attualmente lo standard è HTML5, creato per lpoter implementare feature semantiche.
 // elenco delle feature di html5 // 
 
# Alcuni tag
 li possiamo dividere in 2 categorie
 - contenitori -> possiede il tag   di apertura/chiusura, possono racchiudere testo, nodi testuali, e simil //es <div> //
    - In questa categoria possiamo parlare per es. della <section> //def di essa //
    // es. un ereader potrebbe cercare i <p> per leggere all'utente (quindi da inserire magari per non perdere l'utenza.
    //Oppure un web crawler che cerca uno specifico tag. 
    //Google per es. ha uno standard per cui le pagine non vengono indicizzate se non hanno tag specific//
    
 - vuoti -> Non hanno un tag chiusura. (es. <img>). Non hanno contenuto testuale
    - <img> ->  per es per dire l'immagine da usare si usano degli attributi
                // ogni tag ha degli attributi (alcuni opzionali e altri obbliagtori//
                // img per dire usa src per speciifcare il path , alt invece descrive l'mmagine. Per es gli ereader o strumenti AI usano questi parametri 
    - <input> -> //
    

Possiamo anche dividerli in altri 2 tipi

- Tag di Blocco : Iniaziano sempre su una new line (occupano la larghezza max disponibile)
                    per dire fuori da <h1></h1> và a capo il broswer

- Tag di Linea : renderizzati in sequenza sulla riga corrente (senza andare a capo) 
    - <span> -> // def //
    - <a> -> ancora // def //
    - <img> -> // //

## Stuttura di una pagina HTML 
// code della slide 5 // 

// il prof spiega ogni tag (es. <head> cosa fà e cosa richiede come attributi ecc ecc ) //
        <title> lo usa per far vedere la label nella scheda del browser 

<body> -> // def // è obbligatorio 

## Attributi
sono dichiarate dentro i tag di apertura di un elemento
<tag nome_attributo="valore">Contenuto</tag>
alcuni modificano l'aspetto altri invece hanno funzionalità. 
VIETATO usare attributi per l'aspetto, la good practice è usarli nel CSS (errore grave per il prof).

// esercizio slide 8 //
// ci soffermiamo su questa struttura e poi fino a slide 11

## Liste 
Per organizzare e strutturare le info
ce ne sono 3:
 - Ordinate-> <ol></ol>elementi ordinati con numeeazioni sequenziali (num, alfabeti o romano)
 - non ordinate-> <ul></ul>  same as before ma senza ordine
 - di descrizione-> // def //
 
### Nesting
Le liste possono essere annidate ricorsivamente inserendo un sotto blocco dentro un tag <li>

### Citazione <q> vs <blockquote>
q -> in line si usa per le citazioni, i browser inseriscono automaticamente degli elementi quandol ovedono
blockquote -> cit più lunghe và a capo e supporta l'attributo cite per inserire un URL (fonte per dire)
// il prof parla di <br> e di cosa fà // 

// esc slide 16 // 

# CSS3
// def e slide 17//
## Struct di una regola 
 - Selettore 
    - identifica i DOM a cui applicare la stessa regola
 - Blocco di dichiarazione: 
    - {}
    - una o più dichiarazione dii stile (ognuna con una properietà e val)
    - separazione con ;
    // spiega poi color, font-size e text-align presenti nella slide 18 //
    
## Link 
per il collegamento per i file esterni via tag si usa <link> che stà dentro <head>
// es di code slide 19 // 

## Gestione colori 
 // si parla di color per i colori , slide 20//
 // il più comodo è l'exa, dopo il cancelletto ci stannò i 3 canali (parlava di scale di grigi il prof)

## Font 
// si parla di font, slide 21 // 

## ereditarietà di CSS 
// Leregole di applicano a cascata, si parla del fatto che ad esempio un body con color red poi si applica
//il red a tutti gli elementi a meno che siano specificati di un'altro colore
// si parla  pure del meccanismo di calcolo nel caso di uso di em (slide 23) 